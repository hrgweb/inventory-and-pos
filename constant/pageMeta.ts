// Meta
export const TITTLE = 'Pos and Inventory'

// Pages - Public
export const INDEX = `Home - ${TITTLE}`
export const LOGIN = `Login - ${TITTLE}`
export const POS = `Pos - ${TITTLE}`
export const SIGNUP = `Sign Up - ${TITTLE}`
export const TRIAL_PERIOD = `Trial Period - ${TITTLE}`

// Pages - Admin
export const ADMIN = `Dashboard - ${TITTLE}`
export const ADMIN_PRODUCTS = `Products - ${TITTLE}`
export const ADMIN_SALES = `Sales - ${TITTLE}`
